function [ outAngles ] = airfoilz( atkAngles, liftCoeffs, angles)
dA = diff(atkAngles);
dL = diff(liftCoeffs);
ddAddL = diff(dA)/diff(dL);

% plot X domain based on length(atkAngles)
plot(atkAngles, ddAddL.*ones(1,length(atkAngles)));
hold on

plot(atkAngles, liftCoeffs, 'b*');
outAngles = 

end

